class PokemonModel {
  String name;
  String url;
  //PokemonModel(this.name, this.url);
  //PokemonModel.conParametros({required this.name, required this.url});
  PokemonModel({required this.name, required this.url});
}
